[project]
name = "my-autox-server"
version = "0.1.0"
description = "A phone agent that chooses instead of generating. Same control flow as jev-ultrafast, but the browser harness is replaced by AutoX.js MCP."
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = ["httpx[http2]>=0.28,<1"]

[project.scripts]
autox = "my_autox_server.demo:main"
autox-run = "my_autox_server.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/my_autox_server"]

[dependency-groups]
dev = ["pytest>=8.4,<9", "ruff>=0.14,<1"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
